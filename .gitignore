node_modules/
dist/
coverage/
.env
.env.*
!.env.example
*.log
.DS_Store

# CI is written but cannot be pushed: the GitHub token in use lacks the
# `workflow` scope, and GitHub rejects any push containing a workflow file.
# Run `gh auth refresh -s workflow`, then remove these two lines and
# `git add -f .github/workflows/ci.yml`.
.github/workflows/ci.yml
